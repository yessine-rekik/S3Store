helm upgrade --install staging.s3store chart -f chart/values/values.staging.yml
helm upgrade --install staging.s3store chart -f chart/values/values.staging.yml

helm repo add grafana https://grafana.github.io/helm-charts
helm repo update

# this will install everything and configred everything including promtail and logs collecting from all pods
helm install loki --namespace=monitoring grafana/loki-stack --set loki.image.tag=2.9.3 --set grafana.enabled=false --set promtail.enabled=true


helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm install prometheus --namespace=monitoring prometheus-community/kube-prometheus-stack
